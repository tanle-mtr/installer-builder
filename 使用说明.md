# 安装包制作器

一个本地运行的 Windows 安装包生成工具（Google Material 风格 UI）。
选择主程序，填写应用信息，一键生成 `setup.exe` 安装包 —— 安装包自带：
**桌面快捷方式、开始菜单快捷方式、开机自启、卸载程序（含"卸载或更改程序"面板注册）**。

架构参考 NSIS / Inno Setup 等开源安装器工具，使用 Python 实现，无需安装额外软件（构建环节使用本机 Python + PyInstaller）。

---

## 目录结构

```
安装包制作器/
├── builder/
│   ├── pack_builder.py              # 生成器主程序（Google 风格 GUI）
│   ├── builder_core.py              # 打包核心：模板渲染 / 图标 / PyInstaller 构建
│   ├── templates/
│   │   ├── installer_template.py    # 安装器源码模板（最终打成 setup.exe）
│   │   └── uninstaller_template.py  # 卸载器源码模板（最终打成 uninstall.exe）
│   ├── run.bat                      # 双击运行生成器（需 Python 3.9+）
│   └── build_gui.bat                # 把生成器本身打包成独立 exe
└── 使用说明.md
```

## 环境要求

- Windows 10 / 11
- Python 3.9+（已加入 PATH）
- PyInstaller（`pip install pyinstaller`；运行生成器时若缺失会自动提示）

## 快速开始

1. 双击 `builder\run.bat`（或在 `builder` 目录执行 `python pack_builder.py`）。
2. 填写 **应用信息**（名称、版本、公司/作者）。
3. **选择主程序**，支持两种——
   - **现成程序**：选择你的 `.exe` 文件；
   - **Python 程序**：选择入口脚本（`.py` / `.pyw`），工具会先用 PyInstaller 打包为独立 exe（壳），再制作安装包。
   如程序还依赖其他文件/文件夹，通过"添加"加入附加列表（Python 模式下将内嵌进主程序 exe）。
4. 勾选需要的 **安装选项**：
   - 桌面快捷方式
   - 开始菜单快捷方式
   - 开机时自动启动主程序
5. 可选：自定义 **图标**（`.ico/.png/.jpg`，留空使用默认图标）；选择 **输出目录**。
6. 点击 **生成安装包**，等待构建完成（约 1~3 分钟）。

## 产物说明

输出目录中会生成：

| 文件 | 说明 |
|---|---|
| `setup_应用名.exe` | 安装程序，双击即可安装 |
| `uninstall.exe` | 卸载程序（安装时自动部署到安装目录，无需手动分发） |
| `安装信息.json` | 本次构建的元信息 |

### 安装行为

- 默认安装到 `%LOCALAPPDATA%\Programs\应用名`（当前用户安装，无需管理员权限），安装时可修改路径。
- 按勾选创建 **桌面 / 开始菜单快捷方式**（指向主程序，使用应用图标）。
- 勾选"开机自启"时写入 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`。
- 写入 `HKCU\...\Uninstall\应用名` 注册项，可在 Windows **设置 → 应用 → 已安装的应用** 中看到并卸载。
- 支持静默安装：`setup_应用名.exe --silent`（使用默认配置直接安装）。

### 卸载行为

- 通过控制面板/设置卸载，或运行安装目录下的 `uninstall.exe`。
- 删除：桌面/开始菜单快捷方式、开机自启注册项、卸载注册项、整个安装目录。
- 支持静默卸载：`uninstall.exe --silent`。

## 把生成器打包成独立 exe（可选）

双击 `builder\build_gui.bat`，产物为 `dist\安装包制作器.exe`，可脱离 Python 环境直接运行。

## 常见问题

- **构建失败/卡住**：构建日志会显示 PyInstaller 输出。确认 `pip install pyinstaller` 成功，输出目录有写权限。
- **安装被杀毒软件拦截**：自研安装包无数字签名，属正常现象；选择"仍要运行"即可。
- **想默认关闭自启**：生成时取消勾选"开机自启"即可。
- **多文件程序**：主程序 + 所有依赖文件/文件夹均加入"附加文件/文件夹"列表，安装后目录结构与打包前一致。
- **Python 程序依赖**：打包由本机装有 PyInstaller 的 Python 完成；入口脚本若依赖第三方库，请确保该解释器已安装相应库（PyInstaller 会自动收集）。
- **Python 模式的数据文件**：附加文件/文件夹会内嵌进主程序 exe，程序内用 `sys._MEIPASS` 目录访问（PyInstaller 标准做法）。
